# Dependencies for training the LSTM (02_SRC/Multivariate_forecasting.py).
# Not used in the deployed app — predictions are pre-computed and committed to outputs/.
-r requirements-precompute.txt
tensorflow==2.20.0
h5py
matplotlib
